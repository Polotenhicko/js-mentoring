# Главы 1, 2

## Сколько типов данных в JS?

    Примитивные:
    1. Number
    2. String
    3. Bool
    4. Null
    5. Undefined
    6. Symbol
    7. Bigint
    
    Не примитивный:
    8. Object
***


## Что возвращают и как работают операторы `> >= < <= == === && || ! !!`?
    - `>` - Если левый операнд больше правого, возвращает ***true***.
    - `>=` - Если левый операнд больше или равен правому, возвращает ***true***.
    - `<` - Если правый операнд больше левого, возвращает ***true***.
    - `<=` - Если правый операнд больше или равен левому, возвращает ***true***.
    - `==` - Если у операндов разные типы, приводит их и если после этого они равны, возвращает ***true***.
    - `===` - Если без приведения типов операнды равны, возвращает ***true***.
    - `&&` - Возвращает первый ***falsy*** операнд, иначе последний ***truthy***.
    - `||` - Возвращает первый ***truthy*** операнд, иначе последний ***falsy***.
    - `!` - Приводит операнд к булевому значению и возвращает обратное.
    - `!!` - Приводит операнд к булевому значению.
***


## Как работают сравнения на разных типах?
    Типы приводятся к числу, после чего происходит сравнение.
***


## Сравнения между undefined, null, NaN
    `NaN` - в сравнении всегда возвращает ***false***
    `undefined == null` - ***true***
    `undefined === null` - ***false***
***


## Почему switch не называется циклом?
    `Switch` - это управление вариантами, а цикл должен повторять выполнение блока кода 
***


## Что такое функция?
    Функция это блок кода, к которому можно обращаться и выполнять.
***


# Главы 3, 4

## Что такое this?
    Ссылка, зависящая от контекста - отсылает к родительскому объекту.
***


## Как "потерять" this?
    1. Можно потерять `this`, если вызвать его во вложенной функции.
    2. Можно потерять `this`, если использовать метод с `this` как **callback**.
***


## Как "не терять" this?
    Использовать стрелочные функции или метод `bind()`.
***


## Чему равен this в свойствах объекта?
    Родительскому эл-ту, то есть самому объекту.
***


## Чему равен this в геттерах/сеттерах объекта?
    this в геттерах/сеттерах объекта равен объекту, в котором вызваны асессоры.
***


## Чему равен this внутри функции-стрелки?
    `this` игнорирует функции-стрелки и ссылается на родительский эл-т **не стрелочной** функции на уровень выше.
***


## Как сделать, чтобы объект:
### Был равен определенному числу?
    Создать в объекте метод `valueOf()` или изменить метод [Symbol.toPrimitive](), чтобы возвращать определенное число.

### Был равен определенной строке?
    Создать в объекте метод `toString()` или изменить метод [Symbol.toPrimitive](), чтобы возвращать определенную строку.

### Был не равен сам себе?
    Скопировать его с помощью метода `Object.assign()`
***



# Главы 5, 6

## Как работают вызовы методов у примитивов?
    Примитив оборачивается в объект-конструктор с методами, а затем, после выполнения, оболочка моментально удаляется
***


## Как можно классифицировать методы массивов?
    Мутирующие - не мутирующие.
    Для трансформации, для поиска, для перебора.
***


## Чем отличается Map от объекта?
    Map может иметь св-ва с ключами любого типа.
    У Map свои методы.
***


## Чем отличается WeakMap от Map?
    WeakMap не обеспечивает достижимость объектам, которые используются как ключи.
    Ключи WeakMap могут быть только объектами.
    WeakMap невозможно итерировать.
***


## Что такое замыкание?
    Возможность функции обращаться к тому контексту, в котором она была создана.
***


## На чем и когда замыкается функция?
    При объявлении функция замыкается на родительский объект.
***


## Что такое рекурсия?
    Многократный вызов функцией самой себя.
***


## Как можно оптимизировать рекурсию?
    Можно создать проверку на глубину рекурсии для предвосхищения переполнения стака.
***


## Что такое синхронный и асинхронный код?
    Синхронный - выполняющийся сразу. Асинхронный - выполнение отложено.
***



# Глава 7

## Чем отличается геттер от свойства?
    Геттер не имеет флагов дескриптора writable и value.
    Геттер реализует чтение св-ва.
***


## Как можно классифицировать дескрипторы?
    Дескрипторы свойств-асессоров и дескрипторы свойств.
***


## Какие дескрипторы можно использовать вместе?
    value, writable, enumerable, configurable   или   getter, setter, enumerable, configurable
***


## Какие дескрипторы нельзя использовать вместе?
    getter, setter   и   value, writable
***


## Можно ли использовать this в геттере?
    Да.
***


## Можно ли использовать this в свойстве?
    Да.
***


## Можно ли использовать this в дескрипторе геттера?
    Можно но он будет ссылаться на объект дескриптора.
***



# Глава 8

## Как осуществить прототипное наследование?
    С помощью синтаксиса `extends`.
***


## Как осуществить наследование свойства в this?
    -
***


## Как создать объект без прототипа?
    `Object.create(null)`   или   `obj.__proto__ = null`.
***


## Как изменить прототип у созданного объекта?
    `obj.__proto__ = null`.
***


## Как получить прототип у объекта/числа/строки/функции?
    `obj.__proto__` или Object.getPrototypeOf(obj).
***



# Глава 9

## Что значит "синтаксический сахар"?
    Синтаксический сахар это нововведения в язык, которые не меняют логики, но упрощают читаемость.
***


## Классы это синтаксический сахар?
    Нет, синтаксис `class` добавляет новое св-во функциям - [[isClassConstructor]].
    Классы нельзя вызывать без `new`.
    Код внутри класса всегла 'use strict'.
***


## Сахар над чем? Привести аналогии по синтаксису
    -
***


## Какие есть виды полей в классах?
    -
***


## Какие поля пишутся в прототип, а какие в инстанс?
    -
***


## Можно ли обратиться к статическому полю из this?
    -
***


## Можно ли переопределить метод в дочернем классе?
    -
***


## Можно ли переопределить метод в инстансе класса?
    -
***